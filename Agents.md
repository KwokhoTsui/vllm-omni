# Repository Agent Notes

- When a PR needs visual evidence such as images or videos, add those assets to the PR description or a PR comment using GitHub-hosted attachment links. Do not commit generated image or video artifacts into the codebase unless the user explicitly asks for them to be part of the repository.
